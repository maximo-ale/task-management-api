export interface ProtectedUserInfo{
    id: string,
    name: string,
}

export interface PartialBoardInfo{
    id: string,
    title: string,
}

export interface UserPopulated{
    _id: string,
    name: string,
}